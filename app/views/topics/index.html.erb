<h1>Topics</h1>
<% Topic.all.each do |topic| %>
  <p>
    <h3>#<%= topic.title %></h3>
    <% topic.bookmarks.each do |bookmark| %>
      <p> <%= link_to bookmark.title, bookmark.url %></p>
    <% end %>
  </p>
<% end %>